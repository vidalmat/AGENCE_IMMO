{% extends 'base.html.twig' %}

{% block title %}Page Espace-Membre{% endblock %}

{% block body %}

<section>
    <p class="fildar">abri-nature-immobilier / <a href="{{path("home")}}">accueil /</a><span class="span"> mon espace</span></p>
    <div class="container_espace_membre">
        <div class="sidebar">
            <p>Client : {{ app.user.email }}</p>
            {# {{ dump(app.user) }} #}
                <li><a href="{{path("home")}}">Accueil</a></li>
                <li><a href="#">Messages</a></li>
                <li><a href="{{path("rdv")}}">Rdv</a></li>
                <li><a href="#">Favoris</a></li>
                <li><a href="{{path("calendrier")}}">Visites</a></li>
                <li><a href="{{path("app_logout")}}">Déconnexion</a></li>
            </ul>
        </div>
        <div class="bloc_espace_membre">
            <a href="{{path("home")}}"><img src="../images/logo_espace_client/home.png"><p>Accueil</p></a>
            <a href="#"><img src="../images/logo_espace_client/logo_message.png"><p>Messages</p></a>
            <a href="{{path("rdv")}}"><img src="../images/logo_espace_client/logo_rdv.png"><p>Rdv</p></a>
            <a href="#"><img src="../images/logo_espace_client/etoile.png"><p>Favoris</p></a>
            <a href="{{path("calendrier")}}"><img src="../images/logo_espace_client/gps.png"><p>Visites</p></a>
            <a href="{{path("app_logout")}}"><img src="../images/logo_espace_client/logo_deconnexion.png"><p>Déconnexion</p></a>
        </div>
    </div>
    

    {# <p> Rôles : </p>
    {% for role in app.user.roles %}
       <p>{{ role }}</p> 
    {% endfor %} #}
</section>
{% endblock %}
